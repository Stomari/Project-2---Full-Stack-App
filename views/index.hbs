<div class="index-background d-flex flex-column">
  <div class="d-flex justify-content-center align-items-center cards-margin">
    <div class="index-header">
      <h2>Welcome to <span class="myNavColor">MusiKick</span></h2>
      <p>Find people to play together!</p>
      <p>Create your own band!</p>
      <p>Become a rated user by gaining votes!</p>
    </div>
  </div>


  <div class="d-flex flex-column cards-margin user-ranking ">
    <h2 class="d-flex align-self-center ranked-view-margin">View our ranked members profile</h2>
    <div class="d-flex justify-content-around  flex-wrap">
      {{#each users}}
      <div class="card card-radius" style="width: 20rem;">
        <img src="{{profilePic.path}}" class="card-img-top card-img-size" alt="profile-image">
        <div class="card-body d-flex flex-column">
          <h5 class="card-title"><strong>{{name}}</strong> - rating: {{vote}}</h5>
          <p><strong>Instruments:</strong></p>
          {{#each instruments}}
          <p class="card-text">{{this}}</p>
          {{/each}}
          <div class="d-flex flex-column align-items-center justify-content-end card-link-size">
            <a href="/profile/{{_id}}" class="btn btn-primary justify-content-center">Profile</a>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
  </div>

  <div class="d-flex fixed-bottom justify-content-center footer-background-color">
    <p>Copyrighted by <img class="footer-img" src="../imgs/copyright.png" alt=""> Gabriel Holanda & Sérgio Festa</p>
  </div>
</div>